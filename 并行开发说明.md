# 黔海并行开发说明

这份说明给项目成员使用。目标是让两个人同时开发不同页面，互不覆盖。

## 当前共同起点

- GitHub 仓库：`https://github.com/changjulia/qianhai`
- 平台管理基线分支：`codex/platform-management`
- 当前完成范围：组织架构、权限与账号、数据管理、系统与安全
- 当前仍是交互原型，页面数据为演示数据，尚未真实连接 ERP、CRM 或广告平台

## 推荐分工

| 人员 | 工作内容 | 建议分支 | 主要文件 |
|---|---|---|---|
| 当前开发方 | 收入归因 | `codex/revenue-attribution` | 新建 `app/components/revenue-attribution.tsx` |
| 协作伙伴 | 经营任务与真实目标 | `feature/growth-mission` | 新建 `app/components/growth-mission.tsx` |
| 已完成基线 | 平台管理 | `codex/platform-management` | `app/components/platform-management.tsx` |

不要让两个人同时大范围修改 `app/page.tsx`。新页面应先写在各自独立文件里，最后只在 `page.tsx` 增加一个入口。

## 协作伙伴如何开始

推荐使用 GitHub Desktop：

1. 接受仓库协作者邀请。
2. 克隆 `changjulia/qianhai` 仓库。
3. 切换到 `codex/platform-management` 分支，确认看到平台管理版本。
4. 从该分支创建自己的 `feature/growth-mission` 分支。
5. 只在自己的分支开发并提交，不直接覆盖对方分支。
6. 完成后发起 Pull Request，由双方查看页面后再合并。

## 本地查看页面

进入 `product-prototype` 目录后：

```text
npm install
npm run dev
```

浏览器打开 `http://localhost:3000`。

## 每次交付前检查

```text
npm run lint
npx tsc -p tsconfig.check.json --noEmit
npm run build
```

三项都通过后再提交。若修改了共享导航或公共样式，要在 Pull Request 中明确说明，提醒另一位开发者同步。

## 合并原则

- 平台管理、经营任务、收入归因分别在独立页面文件中开发。
- 公共组件位于 `app/components/shared-ui.tsx`，修改前先在团队中说明。
- 不直接向主分支强行推送；使用 Pull Request 留下讨论和回退记录。
- 合并后重新运行完整构建，并人工点击所有一级导航。
